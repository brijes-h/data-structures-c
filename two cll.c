// create two circular linked lists 
// two head pointers - head1 and head2
// insert() function -> arguments: head and data

#include <stdio.h>
#include <stdlib.h>

int main()
{
    int n1, val1, n2, val2;
    printf("Enter the number of elements: ");
    scanf("%d", &n1);
    for(int i=0; i<n1; i++){
        printf("Enter element %d: ", i+1);
    }
    return 0;
}